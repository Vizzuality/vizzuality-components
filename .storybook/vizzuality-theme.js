import { themes } from '@storybook/theming';
import { create } from '@storybook/theming/create';

export default create({
  base: themes.normal,
  brandTitle: 'Vizzuality components',
  brandUrl: 'https://github.com/Vizzuality/vizzuality-components',
});
